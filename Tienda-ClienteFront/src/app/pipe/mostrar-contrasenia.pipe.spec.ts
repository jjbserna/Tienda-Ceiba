import { MostrarContraseniaPipe } from './mostrar-contrasenia.pipe';

describe('MostrarContraseniaPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarContraseniaPipe();
    expect(pipe).toBeTruthy();
  });
});
